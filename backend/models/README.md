# AI model

Put the trained YOLO/PyTorch model here as:

```text
backend/models/best.pt
```

The `.pt` file is intentionally ignored by Git because it is usually large and may be trained/private data.
